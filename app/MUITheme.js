// src/theme.js or app/theme.js (depending on your project structure)
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002138",
    },
    secondary: {
      main: "#002138",
    },
    error: {
      main: "#f73206",
    },
  },
});

export default theme;
